Copy to secrets/forgejo_token (no extension) and replace this with your
actual Forgejo API token — no trailing newline needed, config.go trims one
if present. See README.md for what this token needs to be able to do.
