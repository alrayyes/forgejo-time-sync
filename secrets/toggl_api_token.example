Copy to secrets/toggl_api_token (no extension) and replace this with your
actual Toggl Focus (2.0) API personal key (starts with toggl_sk_) — no
trailing newline needed, config.go trims one if present. See README.md for
where to find it.
